package customer.apnacare.in.customer.activity;import android.os.Bundle;import android.util.Log;import java.util.Date;import butterknife.BindView;import butterknife.ButterKnife;import co.moonmonkeylabs.realmrecyclerview.RealmRecyclerView;import customer.apnacare.in.customer.R;import customer.apnacare.in.customer.adapters.CasesListAdapter;import customer.apnacare.in.customer.model.CaseRecord;import customer.apnacare.in.customer.utils.Constants;import io.realm.Realm;import io.realm.RealmResults;/** * Created by root on 27/12/16. */public class CasesActivity extends BaseActivity {    @BindView(R.id.casesList)    RealmRecyclerView casesListRecycler;    private Realm realm;    CasesListAdapter casesListAdapter;    int carePlanID;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_case);        ButterKnife.bind(this);        setUpNavigation("Cases - All");        carePlanID = 1;        realm = Realm.getDefaultInstance();        CaseRecord caseRecord = new CaseRecord();        long id = 0;        if(realm.where(CaseRecord.class).max("id") != null){            id = realm.where(CaseRecord.class).max("id").longValue();        }        caseRecord.setId(id + 1);        caseRecord.setCrnNumber("CRN-161220789");        caseRecord.setPatientId(1);        caseRecord.setCareplanName("Short Term Nurse");        caseRecord.setMedicalConditions("Fever, Cold");        caseRecord.setStatus("Pending");        caseRecord.setCreatedAt(new Date());        realm.beginTransaction();        //realm.where(Visit.class).findAll().deleteAllFromRealm();        realm.copyToRealmOrUpdate(caseRecord);        realm.commitTransaction();        RealmResults<CaseRecord> casesList = realm.where(CaseRecord.class).findAll();        Log.v(Constants.TAG,"casesList size: "+casesList.size());        Log.v(Constants.TAG,"casesListt size: "+casesList.toString());        casesListAdapter = new CasesListAdapter(this, casesList, true, true);        casesListRecycler.setAdapter(casesListAdapter);    }    @Override    public void onDestroy() {        super.onDestroy();        realm.close();        realm = null;    }}